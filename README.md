# Veredito Frontend

## Como rodar 

O projeto está configurado com três tipos de build: dev, stg (staging) e prod (production). Para rodar basta passar escolher um flavor (tipo de build). 

```
flutter run --flavor dev 
```